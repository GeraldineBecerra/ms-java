spring:
  application:
    name: ms-usuarios
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  config:
    import: configserver:http://localhost:8081  # Importa la configuración desde el Config Server
